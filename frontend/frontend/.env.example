# Base URL of the PULLI FastAPI backend (api/main.py).
# Required for the /detect page (image upload, detection, analysis,
# reconstruction) to reach a real backend instead of falling back to
# http://localhost:8000, which only works for local development.
VITE_API_BASE_URL=http://localhost:8000
