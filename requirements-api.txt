# FastAPI REST service (api/). Needs BOTH the classical detector
# (engine/, via requirements-engine.txt) and the ML detector
# (experiments/m4_1, m4_2, via requirements-ml.txt) in the same
# process -- see api/main.py's module docstring for why
# KMP_DUPLICATE_LIB_OK is required when both are loaded together.
# This is the complete dependency set for the deployed API container.

-r requirements-ml.txt

fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.26
sqlalchemy
bcrypt
python-dotenv

# Postgres driver for DATABASE_URL=postgresql+psycopg2://... in
# production (api/db/database.py, api/auth/db.py). Binary wheel --
# no local libpq/build toolchain required. SQLite (local dev default)
# needs no driver -- it's in the Python standard library.
psycopg2-binary==2.9.10

# Schema migrations (api/db/models.py) -- see alembic/README.md.
alembic==1.14.0

# S3-compatible client for Cloudflare R2 (api/storage/r2.py). Imported
# lazily -- only instantiated when STORAGE_PROVIDER=r2; local dev
# (STORAGE_PROVIDER=local, the default) never touches this.
boto3==1.35.74
