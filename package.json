{
  "name": "pulli",
  "private": true,
  "version": "0.1.0",
  "description": "Single-command local dev launcher for the PULLI stack (FastAPI backend + React/Vite frontend). Does not replace frontend/frontend/package.json or requirements.txt -- see README.md.",
  "scripts": {
    "predev": "node scripts/preflight.js",
    "dev": "npx concurrently -n API,FRONTEND,ML -c blue,green,yellow --prefix \"[{name}]\" \"npm:dev:api\" \"npm:dev:frontend\" \"npm:dev:ml\"",
    "dev:api": "python -m uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "dev:frontend": "cross-env VITE_API_BASE_URL=http://localhost:8000 npm run dev --prefix frontend/frontend",
    "dev:ml": "node scripts/healthcheck.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "cross-env": "^10.1.0"
  }
}
